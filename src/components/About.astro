---
import { Award, Drill, Coffee, Code2, Braces } from "lucide-react";
import { Image } from "astro:assets";
import avatar from "../assets/me.png";
---

<section id="about" class="section-padding bg-slate-900/50">
  <div class="container-custom">
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-bold mb-6">
        À propos de <span class="gradient-text">moi</span>
      </h2>
      <p class="text-xl text-slate-400 max-w-2xl mx-auto">
        Découvrez mon parcours, ma passion pour le développement et ce qui me
        motive au quotidien.
      </p>
    </div>

    <div class="grid lg:grid-cols-2 gap-16 items-center">
      <!-- Left Column - Text Content -->
      <div class="space-y-6">
        <div class="animate-fade-in-left opacity-0">
          <h3 class="text-2xl font-semibold mb-4 text-white">
            Passionné par l'innovation technologique
          </h3>
          <p class="text-slate-300 leading-relaxed mb-6">
            Développeur Fullstack spécialisé en Symfony / Sylius, rigoureux,
            force de proposition, très curieux et autonome. Je développe et
            optimise des applications web fiables en apportant des solutions
            efficaces à des problématiques complexes.
          </p>
          <p class="text-slate-300 leading-relaxed mb-6">
            Spécialisé dans l’écosystème PHP, et plus particulièrement sur
            Symfony et Sylius, j’accompagne les entreprises dans la création et
            l’évolution de plateformes robustes, modulaires et évolutives.
          </p>
        </div>

        <!-- Skills Highlights -->
        <div class="animate-fade-in-left opacity-0 animation-delay-200">
          <h4 class="text-xl font-semibold mb-4 text-white">
            Ce que j'aime faire
          </h4>
          <div class="space-y-3">
            <div class="flex items-center gap-3">
              <div class="w-2 h-2 bg-blue-500 rounded-full"></div>
              <span class="text-slate-300"
                >Transformer un besoin en solution</span
              >
            </div>
            <div class="flex items-center gap-3">
              <div class="w-2 h-2 bg-purple-500 rounded-full"></div>
              <span class="text-slate-300"
                >Optimisation des flux et des performances</span
              >
            </div>
            <div class="flex items-center gap-3">
              <div class="w-2 h-2 bg-pink-500 rounded-full"></div>
              <span class="text-slate-300"
                >Architecture et patterns de conception</span
              >
            </div>
            <div class="flex items-center gap-3">
              <div class="w-2 h-2 bg-green-500 rounded-full"></div>
              <span class="text-slate-300">Mentorat et formation d'équipes</span
              >
            </div>
          </div>
        </div>
      </div>

      <!-- Right Column - Stats and Image -->
      <div class="space-y-8">
        <!-- Profile Image Placeholder -->
        <div class="animate-fade-in-right opacity-0">
          <div
            class="relative mx-auto w-80 h-80 rounded-2xl bg-gradient-to-br from-blue-500 via-purple-500 to-pink-500 p-1"
          >
            <div
              class="w-full h-full rounded-2xl bg-slate-800 flex items-center justify-center"
            >
              <div class="text-6xl text-slate-600">
                <Image src={avatar} alt="Avatar" class="w-full h-full" />
              </div>
            </div>
          </div>
        </div>

        <!-- Stats Grid -->
        <div
          class="animate-fade-in-right opacity-0 animation-delay-200 grid grid-cols-2 gap-6"
        >
          <div class="bg-slate-800/50 rounded-xl p-6 text-center card-hover">
            <Award className="w-8 h-8 text-blue-400 mx-auto mb-3" />
            <div class="text-3xl font-bold text-white mb-1">5</div>
            <div class="text-slate-400 text-sm">Années Symfony</div>
          </div>

          <div class="bg-slate-800/50 rounded-xl p-6 text-center card-hover">
            <Drill className="w-8 h-8 text-purple-400 mx-auto mb-3" />
            <div class="text-3xl font-bold text-white mb-1">1001</div>
            <div class="text-slate-400 text-sm">Creusage de méninges</div>
          </div>

          <div class="bg-slate-800/50 rounded-xl p-6 text-center card-hover">
            <Braces className="w-8 h-8 text-pink-400 mx-auto mb-3" />
            <div class="text-3xl font-bold text-white mb-1">3</div>
            <div class="text-slate-400 text-sm">Années Sylius</div>
          </div>

          <div class="bg-slate-800/50 rounded-xl p-6 text-center card-hover">
            <Code2 className="w-8 h-8 text-green-400 mx-auto mb-3" />
            <div class="text-3xl font-bold text-white mb-1">5</div>
            <div class="text-slate-400 text-sm">Années d'expérience</div>
          </div>
        </div>
      </div>
    </div>

    <!-- Philosophy Section -->
    <div class="mt-20 text-center">
      <div class="animate-fade-in-up opacity-0 animation-delay-400">
        <h3 class="text-2xl font-semibold mb-6 text-white">Ma philosophie</h3>
        <blockquote
          class="text-xl text-slate-300 italic max-w-4xl mx-auto leading-relaxed"
        >
          "Coder n'est pas une finalité, ce n'est seulement qu'un moyen de
          trouver des solutions à des problématiques complexes."
        </blockquote>
        <div class="mt-6">
          <div
            class="w-16 h-1 bg-gradient-to-r from-blue-500 to-purple-500 mx-auto rounded-full"
          >
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  // Counter animation for stats
  document.addEventListener("DOMContentLoaded", () => {
    const observerOptions = {
      threshold: 0.5,
      rootMargin: "0px",
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          const counters = entry.target.querySelectorAll(".text-3xl");
          counters.forEach((counter) => {
            const target = parseInt(
              counter.textContent?.replace(/\D/g, "") || "0",
            );
            const suffix = counter.textContent?.replace(/\d/g, "") || "";
            let current = 0;
            const increment = target / 30;

            const updateCounter = () => {
              if (current < target) {
                current += increment;
                counter.textContent = Math.ceil(current) + suffix;
                requestAnimationFrame(updateCounter);
              } else {
                counter.textContent = target + suffix;
              }
            };

            updateCounter();
          });
        }
      });
    }, observerOptions);

    // Observe stats section
    const statsSection = document.querySelector("#about .grid.grid-cols-2");
    if (statsSection) {
      observer.observe(statsSection);
    }
  });
</script>
